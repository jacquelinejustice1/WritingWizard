package com.example.writingwizard;

import javafx.scene.control.TextField;

public class Manager {

    //Create Account Functions
    /*
    public String createUser(){
    }

    I need the following functions for alerts on the createAccount page to send to the user

    public boolean validateUserName(String username, String usernameReenter){
     makes sure the username and the username reentered are the same
     and makes sure that it meets the requirements if we want them
      (characters long or numbers)


     true if the same and meet requirements
     false if otherwise

    }

    public boolean validatePassword(String password, String password){
    password reentered and password are the same
    true if they are, false if otherwise
    }

    public boolean validateSecurityQuestions(String sec1, String sec2, String sec3){
    returns true if they are all under 20 characters long, contain no numbers maybe
    otherwise returns false if they are too big and are not real answers
    }

     */

    //Login Functions
    /*
    To alert the user if the account is valid or not


    public boolean validateLogin(String username, String password){
    validates user log in
    returns true if user
    return false if otherwise

    }

    */

    /*
    View-Only functions
     public static String openFile(File filePath, TextArea document){
     opens text file from user that shared the text files to the document

     }
    Main text editor functions
    */

    /*
    public static String openFile(File filePath, TextArea document){
    opens a user's already made documents into the textarea to edit
    }

    public static void createFile(File filepath, TextArea document){
    creates a file from the content in the textarea
    }
     */

    //share screen




}
